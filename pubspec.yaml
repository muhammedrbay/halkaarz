name: halka_arz
description: "Halka Arz Takip ve Portföy Asistanı"
publish_to: 'none'
version: 1.0.1+2

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase
  firebase_core: ^4.4.0
  firebase_messaging: ^16.1.1
  firebase_database: ^12.1.3

  # Local DB
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Network
  http: ^1.4.0

  # Utils
  intl: ^0.20.2
  google_fonts: ^6.2.1
  fl_chart: ^0.70.2
  flutter_local_notifications: ^20.1.0
  url_launcher: ^6.3.1
  google_mobile_ads: ^5.3.0
  app_tracking_transparency: ^2.0.6
  permission_handler: ^12.0.1
  cloud_firestore: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.13

flutter:
  uses-material-design: true
