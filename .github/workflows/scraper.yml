name: Fetch IPO Data from KAP and Sync to Firestore

on:
  schedule:
    # Günde 3 kere çalışır: 06:00, 12:00 ve 18:00 (UTC)
    - cron: '0 6,12,18 * * *'
  workflow_dispatch: # Manuel tetikleme (GitHub Action üzerinden elle çalıştırabilmek için)

jobs:
  scrape-and-sync:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Proje Kodlarını Çek
        uses: actions/checkout@v4

      - name: 🐍 Python 3 Kurulumu
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: 📦 Gerekli Kütüphaneleri Yükle
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements.txt

      - name: 🚀 KAP Veri Kazıyıcısını (Scraper) Çalıştır
        env:
          # GitHub Settings > Secrets sekmesine FIREBASE_CREDENTIALS adında bir secret eklemelisin.
          # İçine Firebase Admin SDK'dan indirdiğin .json dosyasının tüm içeriğini yapıştıracaksın.
          FIREBASE_CREDENTIALS: ${{ secrets.FIREBASE_CREDENTIALS }}
        run: |
          python backend/kap_scraper.py
